export default class Film {
  constructor(f) {
    this.title = f.title
    this.episode = f.episode_id
    this.openingCrawl = f.opening_crawl
    this.director = f.director
    this.producer = f.producer
    this.releaseDate = f.release_date
    this.characters = f.characters
    this.planets = f.planets
    this.starships = f.starships
    this.vehicles = f.vehicles
    this.species = f.species
    this.url = f.url
    this.id = f.url
  }
}