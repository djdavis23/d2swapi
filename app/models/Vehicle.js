export default class Vehicle {
  constructor(v) {
    this.name = v.name
    this.model = v.model
    this.manufacturer = v.manufacturer
    this.creditCost = v.cost_in_credits
    this.length = v.length
    this.maxSpeed = v.max_atmosphering_speed
    this.crew = v.crew
    this.passengers = v.passengers
    this.cargoCapacity = v.cargo_capacity
    this.consumables = v.consumables
    this.vehicleClass = v.vechicle_class
    this.pilots = v.pilots
    this.films = v.films
    this.url = v.url
    this.id = v.url
  }
}